# === СЕКРЕТЫ (НИКОГДА НЕ КОММИТИТЬ) ===
.env
.env.*
# templates are safe and must stay tracked
!.env.example
*.env
.env.local
.env.production
.env.development.local
.env.test.local
.env.production.local

# Ключи и credentials
keys/
**/keys/
# Devnet setup writes mint authority keypairs outside a source directory.
tokens/*.json
**/tokens/*.json
**/*-keypair.json
**/*_keypair.json
*.pem
*.key
*.p12
*.pfx
auth.json
credentials.json
serviceAccountKey.json
firebase-adminsdk.json

# Solana специфика
.anchor/
test-ledger/
target/deploy/*.json

# === СБОРКА И ЗАВИСИМОСТИ ===
node_modules/
.pnp
.pnp.js
dist/
build/
target/
*.tsbuildinfo
.cache/
.parcel-cache/
.next/
.nuxt/

# === IDE И ОС ===
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# === ВРЕМЕННЫЕ ФАЙЛЫ ===
*.bak
*.backup
*.tmp
*.temp
coverage/
.nyc_output/
storybook-static/

# === AIDER И ДРУГИЕ ИИ-ТУЛЫ ===
.aider*
.claude/*
!.claude/skills/
!.claude/skills/**
.cursor/

# === FIREBASE (если не нужен) ===
.firebase/
.firebase.hosting.*

# === BACKEND СПЕЦИФИКА ===
aof_backend/node_modules/
aof_backend/dist/
aof_backend/*.log
aof_backend/.env*

# === АРХИВЫ И ЛОКАЛЬНЫЙ CLI ===
*.zip
*.tar.zst
solana/

# === КРИТИЧЕСКИЕ ИСКЛЮЧЕНИЯ ===
test-ledger/
frontend.zip
frontend.zip.*
ci-local-logs/

# Compiled backend workers
dist-workers/

# Python check caches
__pycache__/
*.pyc

# watchtower exporter: node_modules is a symlink to aof_backend/node_modules; .env holds the exporter token/salt
watchtower/node_modules
watchtower/.env
watchtower/dist/

# [SECURITY_CHECKLIST #65] Docker secret files (docker-compose.secrets.yml)
secrets/
